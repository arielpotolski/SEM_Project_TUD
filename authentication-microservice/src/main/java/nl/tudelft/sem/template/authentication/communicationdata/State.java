package nl.tudelft.sem.template.authentication.communicationdata;

/**
 * Enum of the state of a notification.
 */
public enum State {
    SCHEDULED,
    STARTED,
    COMPLETED,
    DELAYED,
    DROPPED,
    ACCEPTED,
    REJECTED
}
